[pytest]
pythonpath = .
asyncio_mode = strict
markers =
    no_db: marks tests that do not need a database session (skips DB setup/teardown)
    slow: marks slow tests (deselect with '-m "not slow"')
